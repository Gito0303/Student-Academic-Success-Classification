# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2
python:
  version: 3.10  # Sesuaikan dengan versi Python di runtime.txt

# Set the OS, Python version, and other tools you might need
build:
  os: ubuntu-24.04
  tools:
    python: "3.10"  # Sesuaikan dengan versi Python yang Anda gunakan

# Build documentation in the "docs/" directory with Sphinx
sphinx:
  configuration: Docs/conf.py

# Optionally, but recommended,
# declare the Python requirements required to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
python:
  install:
    - requirements: requirements.txt  # Pastikan ini diaktifkan agar RTD menginstal dependensi dari requirements.txt
